# Coffeebox Uzbekistan — Telegram Lead Bot (Node.js + Telegraf)

Готовый проект бота для быстрого деплоя. Поддерживает кнопки, сбор имени/телефона и фильтрацию лидов, которые пересылаются в группу РОПа.

## Установка (локально)
1. Скопируй репозиторий:
   ```bash
   git clone <repo> || unzip coffeebox-uz-bot.zip
   cd coffeebox-uz-bot
   ```
2. Установи зависимости:
   ```bash
   npm install
   ```
3. Скопируй `.env.example` в `.env` и заполните значения:
   ```
   BOT_TOKEN=тут_твой_токен
   ADMIN_GROUP_ID=-123456789  # ID группы с РОПом
   FORWARD_TYPES=🚀 Хочу 10 кофеен (эксклюзив на город)
   ```
   *Если группы ещё нет* — создай группу, добавь туда бота и сделай её публичной или добавь РОПа. Получить ID группы можно несколькими способами (например, временно отправив сообщение и посмотрев `chat.id` в логах) — инструкция ниже.

4. Запуск:
   ```bash
   npm start
   ```

## Деплой (быстро и бесплатно)
Можно использовать Railway, Render, Fly.io или Vercel (используя Webhook). Для простоты используй Railway или Render (long polling работает).

## Как работает фильтрация
В `.env` параметр `FORWARD_TYPES` — список типов заявок (по тексту кнопки), разделённых запятой. Лиды с типом, входящим в этот список, будут автоматически пересылаться в `ADMIN_GROUP_ID`.

## Структура проекта
- `index.js` — точка входа, загружает переменные окружения и запускает бота
- `bot.js` — логика бота (кнопки, сбор имени/телефона, пересылка)

## Быстрая настройка группы и получения ADMIN_GROUP_ID
1. Создай группу в Telegram (Supergroup рекомендуется).
2. Добавь в неё бота и РОПа.
3. Временно включи логирование чат.id: при запуске бот печатает в консоль `Hint to get group id: send /id in group` — добавь в группу бота и отправь `/id`. В консоли появится `chat.id`.

## Текст сообщений (используемые формулировки)
- Приветствие:
  ```
  Добро пожаловать в Coffeebox Uzbekistan!
  Мы строим сеть автоматических кофеен без бариста и аренды.
  Опыт: 150+ кофеен в Казахстане.

  Что вас интересует?
  ```
- Кнопки:
  • ☕ Хочу 1 кофейню  
  • 🚀 Хочу 10 кофеен (эксклюзив на город)  
  • 📈 Хочу расчёт окупаемости
- Запрос после выбора:
  ```
  Отлично! Кофейня окупается примерно за 12 месяцев.
  Укажите, пожалуйста:
  – Имя
  – Телефон (WhatsApp или Telegram)
  ```

## Лицензия
MIT
